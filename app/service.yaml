apiVersion: v1
kind: Service
metadata:
  name: gpu-inference
  namespace: ai-demo
  labels:
    app: gpu-inference
spec:
  selector:
    app: gpu-inference
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
